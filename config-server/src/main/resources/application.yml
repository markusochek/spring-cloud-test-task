server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/markusochek/spring-cloud-test-task-config.git
          default-label: main
          search-paths: '{application}'